After launching the application navigate to http://localhost:3000/ .
You will see a list of links to all the queries from the first assignment.
Creating collections is not included, since it is not provided by Monk(or at least I was unable to find it).
However, collections are automatically created when a document is inserted in them.
Results are provided as JSON.
